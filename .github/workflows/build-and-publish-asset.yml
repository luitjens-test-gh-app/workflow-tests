name: Build, Publish and Release Teraslice Asset
run-name: ${{ github.actor }} is building, publishing and releasing the Teraslice Asset
on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  call-asset-build:
    if: github.event.pull_request.merged == true
    uses: luitjens-test-gh-app/workflows/.github/workflows/asset-build-and-publish.yml@1128f4615fbc49ebc39a763170580e7e76223a48
    secrets: inherit
