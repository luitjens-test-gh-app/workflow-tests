name: Build, Publish and Release Teraslice Asset
run-name: ${{ github.actor }} is building, publishing and releasing the Teraslice Asset
on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  call-asset-build:
    if: github.event.pull_request.merged == true
    uses: luitjens-test-gh-app/workflows/.github/workflows/asset-build-and-publish.yml@6609674bafbdf9f5f054c8cfd0b3f69263c99608
    secrets: inherit
